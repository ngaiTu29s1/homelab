title: "Tu's Homelab "
subtitle: "This is a ✍️(◔◡◔)"
# documentTitle: "Welcome" # Customize the browser tab text
logo: "/assets/homer-logo.svg"
# Alternatively a fa icon can be provided:
# icon: "fas fa-skull-crossbones"

header: true # Set to false to hide the header
# Optional: Different hotkey for search, defaults to "/"
hotkey:
  search: "Shift"


footer: '<p>👑 Crafted with <span class="has-text-danger">❤️</span> by <strong>Tran Tuan Tu</strong> —Running on a humble homelab powered by Debian, Docker & caffeine ☕ ·
  <a href="https://github.com/ngaiTu29s1" target="_blank"><i class="fab fa-github"></i> GitHub</a></p>' # set false if you want to hide it.

columns: "3" # "auto" or number (must be a factor of 12: 1, 2, 3, 4, 6, 12)
connectivityCheck: true # whether you want to display a message when the apps are not accessible anymore (VPN disconnected for example).
                        # You should set it to true when using an authentication proxy, it also reloads the page when a redirection is detected when checking connectivity.

# Optional: Proxy / hosting option
proxy:
  useCredentials: false # send cookies & authorization headers when fetching service specific data. Set to `true` if you use an authentication proxy. Can be overrided on service level. 
  headers: # send custom headers when fetching service specific data. Can also be set on a service level.
    Test: "Example"
    Test1: "Example1"


# Optional theming
theme: default # 'default' or one of the themes available in 'src/assets/themes'.

# Set the default layout and color scheme
defaults:
  layout: list # Either 'columns', or 'list'
  colorTheme: auto # One of 'auto', 'light', or 'dark'


# Optional custom stylesheet
# Will load custom CSS files. Especially useful for custom icon sets.
stylesheet:
  - "assets/themes/cyber-neon.css" # This is the default neon theme, you can remove it if you don't want to use it.

# Here is the exhaustive list of customization parameters
# However all value are optional and will fallback to default if not set.
# if you want to change only some of the colors, feel free to remove all unused key.
colors:
  light:   # Latte - Đảm bảo độ tương phản tốt và màu sắc dễ chịu
    highlight-primary: "#1e66f5"    # Blue
    highlight-secondary: "#8839ef"  # Purple
    highlight-hover: "#179299"      # Teal
    background: "#eff1f5"           # Nền sáng
    card-background: "#ffffff"      # Card trắng
    text: "#4c4f69"                  # Chữ đen xám (dễ đọc trên nền sáng)
    text-header: "#1e1e2e"           # Chữ tiêu đề đậm
    text-title: "#11111b"            # Chữ tiêu đề chính rất đậm
    text-subtitle: "#6c6f85"         # Chữ phụ đề
    card-shadow: "rgba(0, 0, 0, 0.08)" # Bóng đổ nhẹ
    link: "#1e66f5"                  # Link blue
    link-hover: "#8839ef"            # Link hover purple

  dark:    # Mocha - Các chỉnh sửa để dịu mắt hơn, có chiều sâu
    highlight-primary: "#89b4fa"   # Blue pastel (vẫn giữ)
    highlight-secondary: "#cba6f7" # Purple pastel (vẫn giữ)
    highlight-hover: "#94e2d5"     # Teal pastel (vẫn giữ)
    background: "#0d0d1a"          # Nền tối rất sâu (tối hơn một chút)
    card-background: "#1e1e2e"     # Nền card tối hơn nền background một chút (tạo layer)
    text: "#cdd6f4"                # Chữ sáng nhạt (tương phản tốt với nền tối)
    text-header: "#CCD0DA"         # Chữ tiêu đề đầu sáng, dịu (xám trắng hơi xanh)
    text-title: "#DFE3EB"          # Chữ tiêu đề chính sáng, rõ (trắng sữa)
    text-subtitle: "#A6ADC8"       # Chữ phụ đề xám xanh (hơi sáng hơn để rõ hơn)
    card-shadow: "rgba(0, 0, 0, 0.7)" # Bóng đổ sâu
    link: "#89b4fa"                # Link blue pastel
    link-hover: "#cba6f7"          # Link hover purple pastel


# Optional message
message:
  # url: "https://<my-api-endpoint>" # Can fetch information from an endpoint to override value below.
  # mapping: # allows to map fields from the remote format to the one expected by Homer
  #   title: 'id' # use value from field 'id' as title
  #   content: 'value' # value from field 'value' as content
  # refreshInterval: 10000 # Optional: time interval to refresh message
  #
  # Real example using chucknorris.io for showing Chuck Norris facts as messages:
  # url: https://api.chucknorris.io/jokes/random
  # mapping:
  #   title: 'id'
  #   content: 'value'
  # refreshInterval: 10000
  style: "is-warning"
  title: "Optional message!"
  icon: "fa fa-exclamation-triangle"
  # The content also accepts HTML content, so you can add divs, images or whatever you want to make match your needs.
  content: "Tại sao giờ này chưa ngủ ?."

# Optional navbar
# links: [] # Allows for navbar (dark mode, layout, and search) without any links
links:
  - name: "Github Docs"
    icon: "fab fa-github"
    url: "https://github.com/bastienwirtz/homer"
    target: "_blank" # optional html tag target attribute
  - name: "link 2"
    icon: "fas fa-book"
    url: "https://github.com/bastienwirtz/homer"
  # this will link to a second homer page that will load config from page2.yml and keep default config values as in config.yml file
  # see url field and assets/page.yml used in this example:
  - name: "Second Page"
    icon: "fas fa-file-alt"
    url: "#page2"

services:
  - name: "Monitoring"
    icon: "fas fa-chart-line"
    items:
      - name: "Uptime Kuma"
        logo: "/assets/uptime-kuma-logo.svg"
        subtitle: "Monitor everything"
        url: "http://debian:3001"
        target: "_blank"

      - name: "Netdata"
        logo: "/assets/netdata-logo.svg"
        subtitle: "Live metrics"
        url: "http://debian:19999"
        target: "_blank"

      - name: "Portainer"
        logo: "/assets/portainer-logo.svg"
        subtitle: "Container management"
        url: "http://debian:9000"
        target: "_blank"
        stats: [cpu, mem]

      - name: "Linkding"
        logo: "/assets/linkding-logo.svg"
        subtitle: "Bookmark manager"
        url: "http://debian:9090"
        target: "_blank"
